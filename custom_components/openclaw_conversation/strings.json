{
  "config": {
    "step": {
      "user": {
        "title": "Connect to OpenClaw",
        "description": "Enter your OpenClaw Gateway URL and API token.",
        "data": {
          "name": "Name",
          "base_url": "OpenClaw Gateway URL",
          "api_key": "API Token",
          "model": "Model",
          "timeout": "Timeout (seconds, 0 = no limit)",
          "system_prompt": "System prompt"
        },
        "data_description": {
          "system_prompt": "Instructions sent before each conversation (e.g. keep responses short for voice)",
          "timeout": "Set to 0 to disable the response timeout (recommended for long-running requests)"
        }
      }
    },
    "error": {
      "cannot_connect": "Unable to connect to OpenClaw Gateway. Check the URL.",
      "cannot_reach": "Gateway unreachable. Check that Home Assistant can route to the gateway host (network, firewall, Docker bridge, DNS) and that the port is open.",
      "timeout": "Gateway took too long to respond during validation. Check gateway load or try again.",
      "invalid_auth": "Invalid API token.",
      "forbidden": "Access forbidden by the gateway (403). Check token scopes or gateway auth policy.",
      "endpoint_not_found": "Endpoint /v1/chat/completions not found (404). Check the base URL — it should be the gateway root (e.g. http://host:18789), not include /v1.",
      "endpoint_disabled": "Chat Completions endpoint is not enabled on the Gateway. Add gateway.http.endpoints.chatCompletions.enabled: true to your OpenClaw config.",
      "server_error": "Gateway returned a server error (5xx). Check the gateway logs.",
      "bad_request": "Gateway rejected the request (400). Check that the model name and payload are valid — see the Home Assistant logs for the gateway response body.",
      "model_not_available": "The configured model is not available on this OpenClaw Gateway. Try a model that exists on your gateway (e.g. 'openclaw:main')."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "OpenClaw Conversation Options",
        "data": {
          "model": "Model",
          "timeout": "Timeout (seconds, 0 = no limit)",
          "system_prompt": "System prompt",
          "strip_emoji": "Strip emojis from responses"
        },
        "data_description": {
          "timeout": "Set to 0 to disable the response timeout (recommended for long-running requests)"
        }
      }
    }
  }
}
